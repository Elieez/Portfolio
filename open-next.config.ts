import type { OpenNextConfig } from '@opennextjs/cloudflare';

const config: OpenNextConfig = {
  // Provide the required 'default' property; cast to any if the detailed shape isn't needed here
  default: {} as any
};

export default config;